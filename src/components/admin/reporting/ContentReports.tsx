"use client";

import React from "react";

function ContentReports({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>Content Reports for {institutionId}</p>
    </div>
  );
}

export default ContentReports;
