"use client";

import React from "react";

function AnalyticsReports({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>Analytics for {institutionId}</p>
    </div>
  );
}

export default AnalyticsReports;
