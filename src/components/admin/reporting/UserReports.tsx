"use client";

import React from "react";

function UserReports({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>User Reports for {institutionId}</p>
    </div>
  );
}

export default UserReports;
