"use client";

import React from "react";

function TrainingReports({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>Training Reports for {institutionId}</p>
    </div>
  );
}

export default TrainingReports;
