"use client";

import React from "react";

function AIInsights({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>AI Insights for {institutionId}</p>
    </div>
  );
}

export default AIInsights;
