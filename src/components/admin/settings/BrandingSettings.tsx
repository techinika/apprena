"use client";

import React from "react";

function BrandingSettings({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>Branding Settings for {institutionId}</p>
    </div>
  );
}

export default BrandingSettings;
