"use client";

import React from "react";

function AccessibilitySettings({ institutionId }: { institutionId: string }) {
  return (
    <div>
      <p>Accessibility Settings for {institutionId}</p>
    </div>
  );
}

export default AccessibilitySettings;
