"use client";

import React from "react";

function Discussions({ institutionId }: { institutionId: string }) {
  return (
    <div>
      {/* Shows discussions that were created in topics created by the current organization */}
    </div>
  );
}

export default Discussions;
